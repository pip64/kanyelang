yeezy {
    bleached count be 0;

    # repeat 10 times {
    #     spit count;
    # 
    #     aldi count to count + 1;
    # }

    repeat 10 times {
        aldi count to count + 1;

        drip (count >= 3) {
            spit count;
        } nah { spit "no"; }
    }
}